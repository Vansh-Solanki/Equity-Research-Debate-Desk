# Equity Research Debate Desk — progress

**Status:** Phase 9 complete (Streamlit frontend — boots cleanly, not yet interactively verified in a browser; see Phase 9 write-up). Post-Phase-7 fix: Bull/Bear/Judge now wired to Phase 5's RAG retrieval instead of raw `get_filing` truncation (see Known issues).
**Last updated:** September 3, 2026

## One-line summary

Two AI analyst agents (Bull and Bear) debate a company's real SEC filing using retrieved, fact-checked evidence, a Judge agent scores the debate and writes a memo, and an optional deep-dive mode spawns section-specific sub-agents on demand. Built entirely on free tools, no paid subscriptions.

## Architecture (locked in)

```
Data sources (SEC EDGAR, yfinance, news RSS)
        |
MCP tool layer (get_filing, get_price, get_news)
        |
RAG index (chunk -> embed -> Chroma -> cross-encoder rerank)
        |
Agent crew (Bull, Bear, Judge — CrewAI + Groq)
        |-- Deep-dive spawner (sub-agent per filing section, on demand)
        |
Output layer (memo, Streamlit UI, Langfuse/local logging)
```

Deployment layer (added later, also locked in):
```
Browser -> Streamlit frontend -> FastAPI backend -> Redis job queue -> Worker pool
(worker pool runs the agent crew, calls Groq + Chroma, writes results back)
Hosted as a Docker container on Hugging Face Spaces (free)
```

## Tech stack (all free tier / open source)

| Layer | Tool | Notes |
|---|---|---|
| LLM | Groq API (free tier) | Confirmed as primary driver |
| Agent framework | CrewAI | Defines Bull, Bear, Judge roles |
| Tool access | Self-built MCP server | Wraps EDGAR, yfinance, news as callable tools |
| Filings | SEC EDGAR public API | Free, no key required |
| Price/financial data | yfinance | Free Python package |
| News | Free RSS feeds | Catches recent events not yet in filings |
| Embeddings | sentence-transformers | Local, no API cost |
| Vector store | **ChromaDB** | Chosen over FAISS, Qdrant, LanceDB, etc. — best fit for single-filing scale + metadata filtering for deep-dive sections |
| Reranking | cross-encoder (ms-marco-MiniLM) | Improves fact-check precision over raw vector similarity |
| Hallucination check | NLI entailment model (local, free) | Claim-level groundedness scoring, applied to all agents equally |
| Frontend | Streamlit | Debate feed UI + deep-dive buttons + memo card |
| Backend | FastAPI | Async, handles concurrent requests |
| Job queue | Redis (Upstash free tier) | Decouples request submission from long-running debate jobs |
| Observability | Langfuse (self-hosted or free tier) | Logs every tool call, retrieval, and judge decision |
| Hosting | Hugging Face Spaces (Docker) | Free, credible AI hosting choice |
| CI/CD | GitHub Actions | Free for public repos |

## Concept coverage (against the 13-topic checklist)

| Topic | Covered? | Where |
|---|---|---|
| MCP | Yes | Tool layer wrapping EDGAR/yfinance/news |
| A2A | No | Not part of this project — planned for the second (general) project |
| LangGraph | Optional | Not required by current design |
| CrewAI | Yes | Bull/Bear/Judge roles |
| Claude Agent SDK | No | Planned for the second (general) project |
| Short-term memory | Partial | Per-session debate history; not a deep memory system |
| Context engineering | Yes | Chunking + RAG for long filings |
| Orchestrator-worker | Yes | Deep-dive spawner dispatches section sub-agents |
| Debate/adversarial review | Yes | Core mechanic of the whole project |
| Hierarchical sub-agent spawning | Yes | One sub-agent per filing section, on demand |
| Evaluation/observability | Yes | Claim-level groundedness scoring, Judge F1, debate quality, retrieval recall, latency |
| Safety/guardrails | Yes | Entailment-based fact-checking on every agent's claims |
| Cloud/serverless deployment | Yes | Docker on Hugging Face Spaces, queue-worker pattern |

**Gap:** A2A and Claude Agent SDK are intentionally deferred to the second (general-domain) project.

## Key design decisions log

- **Debate turn order:** Fixed (Bull always first), implemented as an ordered list (`speaking_order = [bull_agent, bear_agent]`) so randomizing later is a one-line change if ever wanted. All logging/labeling is by agent name, never by position.
- **Independent research:** Bull and Bear run separate retrieval queries and write opening statements without seeing each other's statement. From round 2 onward, each agent reads the full transcript and must directly engage with the other's specific points.
- **Deep-dive trigger:** User-initiated only, via one button per filing section (dynamically generated from however many sections that specific filing has), not automatic. Clicking a button spawns exactly one sub-agent for that section; already-dived sections are cached, not rerun.
- **Hallucination checking:** One shared evaluation pipeline (claim extraction -> retrieval -> cross-encoder rerank -> NLI entailment check) applied uniformly to Bull, Bear, the Judge, and deep-dive sub-agents. No agent is exempt or self-certifying.
- **Scope boundary:** Confirmed this project does NOT cover merger and acquisition analysis (single-company research only, not deal/comparison scenarios).
- **Vector DB:** Chroma chosen over FAISS/Qdrant/Weaviate/Milvus/LanceDB/pgvector/Annoy/Redis-vector — best balance of ease of use, metadata filtering, and appropriate scale for this project.
- **Deployment concurrency:** Queue-worker pattern (Redis + worker pool) chosen over a naive single-process Streamlit app specifically to support multiple concurrent users without blocking or session collisions. Acknowledged limitation: real throughput is capped by Groq's free-tier rate limit, not by the architecture itself.

- **Groq model choice:** `llama-3.1-8b-instant` (originally assumed in spec.md) is no longer available on the free-tier account as of Aug 2026. Switched to `openai/gpt-oss-20b`, confirmed working with 1000 requests/day and 8000 tokens/min free-tier limits. Re-check `client.models.list()` if agents start hitting 404s later — Groq's free model lineup changes.

- **MCP SDK version:** Installing `crewai` in Phase 3 pinned `mcp` back down to v1.28.1 (from the v2.1.1 that Phase 2 was built against), which removed `mcp.server.mcpserver.MCPServer`. `mcp_server/server.py` now uses `from mcp.server.fastmcp import FastMCP` instead — decorator usage (`@mcp.tool()`) is unchanged. If `mcp` gets upgraded again later, re-check this import.
- **News source:** Spec suggested free RSS feeds generically; implemented against Google News RSS (`news.google.com/rss/search`) since it needs no API key and works for arbitrary company names, not just tickers.
- **SEC EDGAR user agent:** Set to `Equity Research Debate Desk vsolanki4565@gmail.com` in `.env` — SEC blocks requests with generic/placeholder user agents.
- **Filing text extraction:** Added `beautifulsoup4` (not in original spec's requirements.txt draft) to strip HTML from 10-K documents into plain text for later chunking in Phase 5. Raw text capped at 3M chars as a safety limit.

- **Phase 3 — CrewAI version and Groq quirks (crewai 1.15.18):**
  - `crewai`'s generic litellm-fallback `LLM` class (used for any model without a native provider integration, which includes Groq) stamps every message with a `cache_breakpoint` field intended for Anthropic-style prompt caching, but never strips it for providers that don't understand it. Groq's OpenAI-compatible endpoint strictly rejects unknown message fields and 400s on every call. Worked around in `agents/llm.py` by monkeypatching `crewai.llms.cache.mark_cache_breakpoint` to a no-op — both crewai executors re-import that name locally on each call, so patching the source module's attribute is enough. Revisit if crewai ships a real fix upstream.
  - `crewai` requires `litellm` installed separately as of 1.15.18 (`pip install litellm`) for any model that isn't one of its natively-integrated providers (openai/anthropic/azure/bedrock/gemini/etc.) — added to `requirements.txt`.
  - Groq's free tier (8000 tokens/minute) is tight enough that a ReAct-style agent loop calling multiple tools (get_filing + get_price + get_news) in one task run reliably blows the budget, since each turn resends the full growing conversation history. Fixed by: (1) trimming what each tool returns before it reaches the agent's context (`agents/tools.py` — filing text capped at 2500 chars starting from the actual cover page rather than the XBRL tag-data preamble that precedes it in the stripped HTML, price history capped to the last 5 points, news summaries capped to 200 chars), (2) `max_iter=2` per agent (one tool-call turn, one final-answer turn), (3) each Phase 3 task instructs the agent to call `get_filing` only, skipping `get_price`/`get_news`, and (4) `agents/llm.py`'s `run_with_rate_limit_backoff` retries a `RateLimitError` by sleeping the exact wait time Groq's error message reports. This is a real free-tier constraint, not a one-off bug — worth revisiting once Phase 5's RAG retrieval replaces raw tool dumps with much smaller retrieved chunks, and again in Phase 4 when the debate loop needs multiple sequential agent turns.
  - `get_filing`'s stripped HTML started with a long block of inline-XBRL tag data (fund identifiers, GAAP taxonomy refs) with no prose in it before the actual filing cover page began — for AAPL/MSFT/TSLA the real text started 14k-34k characters in. Initially worked around at the call site (`agents/tools.py` searched for the cover-page marker and truncated from there); **later fixed at the source** in `mcp_server/tools/get_filing.py` — see the "Known issues" entry below for the real fix.

- **Phase 4 — debate loop and Judge agent:**
  - `orchestration/debate_loop.py` holds `speaking_order = [bull_agent, bear_agent]` as the ordered-list pattern spec.md requires; round 1 calls each side's `run_opening_statement` (independent, as in Phase 3), rounds 2+ call `run_rebuttal(company, transcript_text)` which feeds the full transcript-so-far back in and instructs each side to engage the opponent's most recent specific point before adding anything new.
  - `agents/judge_agent.py` is new: reads the finished transcript and scores `stronger_side` / `claims_checked` / `claims_unsupported` / `memo`. Deliberately does its own reading rather than calling any evidence-retrieval tool — real claim-level verification (extraction -> retrieval -> entailment) is Phase 6's shared pipeline, applied uniformly to Bull, Bear, Judge, and deep-dive sub-agents alike; the Judge is not exempt from that later.
  - **New crewai/Groq bug found in Phase 4: a zero-tools agent can still 400.** Giving `run_rebuttal`/the Judge `tools=[]` (the natural way to say "don't call tools") triggers a different code path in crewai's executor (`call_llm_and_parse`, used when `self.original_tools` is empty) that still sends `tool_choice: "none"` to Groq — and `openai/gpt-oss-20b` sometimes emits a tool call anyway (observed calling a hallucinated `browser.get_filing`, and separately a real `get_filing` with a bogus extra arg), which Groq validates strictly and rejects with a 400 (`"Tool choice is none, but model called a tool"`). Worked around by keeping every agent's real tools attached (`tools=ALL_TOOLS`) always, and telling it not to call them via the task description instead — plus `agents/llm.py`'s `run_with_rate_limit_backoff` now also retries this specific `BadRequestError`, since it's model non-determinism (a retry usually doesn't repeat the same errant tool call).
  - **`output_pydantic` costs an extra, unretried LLM call.** The Judge originally used `Task(output_pydantic=JudgeVerdict)` for structured output. crewai converts free text into the pydantic model via a second, separate LLM call (through the `instructor` library) when the model doesn't return native structured output — and that conversion call isn't covered by our `run_with_rate_limit_backoff` wrapper, so it fails hard on any rate limit. Replaced with a plain structured-text format (`STRONGER_SIDE: ...` / `CLAIMS_CHECKED: ...` / etc.) that the Judge is instructed to output and `agents/judge_agent.py`'s `_parse_verdict` parses with a regex — no extra LLM call, no extra failure point.
  - **Groq free tier has a daily cap too, not just per-minute.** `openai/gpt-oss-20b` on this account is capped at both 8000 tokens/minute *and* 200,000 tokens/day. Heavy same-day testing (this Phase 3 + Phase 4 session) ran the daily quota down to ~0 remaining — confirmed via a `RateLimitError` reporting "on tokens per day (TPD): Limit 200000, Used 199965". `run_with_rate_limit_backoff`'s retry-after parser only handled seconds ("Xs"), not the "Xm Y.Zs" format Groq uses for longer (daily-cap) waits, so it was retrying every ~15s against a 20+ minute wait and burning through all 6 attempts uselessly. Fixed: the parser now handles both formats, and any parsed wait over `MAX_SENSIBLE_WAIT_SECONDS` (90s) is treated as the daily cap and fails fast with a clear message instead of blocking or retrying futilely.
  - **Not yet re-verified after the last two fixes** (Judge's plain-text parser, the daily-cap fail-fast): the daily quota ran out before a fresh end-to-end run could confirm them together. The debate loop *was* fully verified end-to-end before those fixes (`scripts/test_phase4_debate.py` on AAPL — 3 rounds, every round-2+ statement named and rebutted a specific opponent claim, Judge produced a verdict), so the core mechanism is proven; re-run `python -m scripts.test_phase4_debate AAPL` once the daily quota resets to confirm the memo no longer truncates.
  - Also bumped `orchestration/debate_loop.py`'s `INTER_TURN_PAUSE_SECONDS` to 15s between agent turns, to spread requests out against the per-minute cap and reduce (not eliminate) how often the retry path is needed at all.

- **Phase 5 — RAG index (Chroma):**
  - `rag/chunker.py`: fixed-window chunking with overlap (1000 chars / 200 overlap, snapped to word boundaries), sized to `all-MiniLM-L6-v2`'s 256-token truncation limit. Not the Item-N section-aware split — that's still Phase 7's `deep_dive/section_splitter.py`, run separately for deep-dive filtering. `chunk_filing()` takes a `get_filing()` data dict and returns records with deterministic ids (`{company}-{filing_type}-{section}-{chunk_index}`, no random component — see the id-collision bug below for why that matters) and metadata (company, filing_type, filed_date, source_url, section, chunk_index).
  - `rag/embedder.py`: local `sentence-transformers` (`all-MiniLM-L6-v2`), no API key, cached as a module-level singleton since loading the model is the slow part.
  - `rag/chroma_store.py`: one persistent Chroma collection per company (`filing_{TICKER}`), not one shared collection with a metadata filter — simple physical isolation, good enough for Phase 5's single-user testing. Phase 10's `orchestration/session.py` adds per-debate-session isolation on top of this later.
  - `rag/reranker.py`: cross-encoder (`cross-encoder/ms-marco-MiniLM-L-6-v2`) reranks Chroma's vector-similarity candidates — slower but more precise than bi-encoder similarity alone, run only over the small candidate set Chroma already narrowed down to.
  - `rag/retriever.py`: `index_company_filing(company, filing_type)` ties chunker+embedder+chroma_store together; `retrieve(query, company, top_k, candidate_k)` ties embedder+chroma_store+reranker together.
  - **Real bug found during acceptance testing: chunk ids had a random UUID suffix, silently breaking "upsert" idempotency.** `chunk_filing()` originally appended `uuid.uuid4().hex[:8]` to every chunk id "to guarantee uniqueness," which meant every call to `index_company_filing()` generated an entirely new set of ids — so `chroma_store.upsert_chunks()`'s upsert-by-id never actually overwrote a previous index, it just kept *adding* duplicate copies of the same filing's chunks (old text alongside new text) every time a company was re-indexed. This went unnoticed until the `get_filing.py` page-footer fix (below) was tested: the retrieval acceptance script re-indexed AAPL and still surfaced pre-fix chunk text containing the old footer noise, because the collection now held both pre-fix and post-fix chunks mixed together and Chroma had no way to know the old ones were stale. Fixed two ways: (1) chunk ids are now fully deterministic (company + filing_type + section + chunk_index, no random component), so a same-length re-index correctly overwrites via upsert; (2) `chroma_store.delete_filing_chunks()` is called before every `index_company_filing()` upsert regardless, since a *shorter* re-index (fewer chunks than before, e.g. after a text-cleaning fix) would still leave the old tail-end chunks orphaned even with deterministic ids. Local `chroma_data/` was wiped and rebuilt clean after this fix — anyone with a pre-existing local Chroma store from before this fix should do the same (`rm -rf chroma_data`) rather than trust upsert to have cleaned it up on its own.
  - **Second real bug, found in `get_filing.py`, surfaced while debugging the above:** AAPL's 10-K (not MSFT's or TSLA's — filer/template-specific) repeats a page-footer string ("`{Company Name}. | {Year} Form {Type} | {Page}`", e.g. "Apple Inc. | 2025 Form 10-K | 3") on every rendered page — confirmed 58 occurrences in one filing. It's pure page furniture with no content, but because it's near-identical across dozens of chunks, it actively hurt retrieval quality: a query landing near a page boundary would match on the shared footer text as easily as on the chunk's real content, pulling in the wrong chunk. Fixed in `mcp_server/tools/get_filing.py`'s new `_strip_page_footers()`, called after `_strip_html()`, using a regex built from the filing's own already-resolved company name (`re.escape(company_name) + r"\.?\s*\|\s*\d{4}\s*Form\s+[\w-]+\s*\|\s*\d+\b"`) so it only ever matches that filing's real footer, never guesses. Verified zero leftover matches on all 7 previously-tested filers (AAPL/MSFT/TSLA/RIOT/NVAX/BABA/TM) and zero regressions (MSFT/TSLA/etc. had 0 matches before and after, since they don't use this footer style).
  - **Acceptance methodology**: no hand-labeled test set exists yet (`evaluation/test_set.json` is Phase 6's job, for claim-level entailment, not retrieval). `scripts/test_phase5_retrieval.py` instead builds a reproducible smoke test from the filing itself — index it, sample chunks spread across the document, treat a snippet from partway through each as a "question," and check whether reranking correctly re-surfaces that exact source chunk in the top 3 out of the company's *entire* chunk set (258 for AAPL, 407 for MSFT). Had to add a distinctiveness filter (snippet must contain a digit or >= 2 proper-noun-like capitalized words) after discovering that a naive random-span sampler frequently landed on genuinely-duplicated legal boilerplate (10-K risk-factor paragraphs routinely close with the identical sentence "...could materially adversely affect the Company's business, reputation, results of operations, financial condition and stock price." verbatim, dozens of times) — no retriever can or should disambiguate between interchangeable instances of the same boilerplate sentence, so scoring those as failures was testing document repetitiveness, not retrieval quality. Final result: 10/10 on both AAPL and MSFT (acceptance bar is >= 8/10).

- **Phase 6 — claim-level evaluation pipeline (hallucination check):**
  - `evaluation/claim_extractor.py`: one Groq call breaks an agent's free-text statement into individual, self-contained factual claims (pronouns resolved to the real company name, hedges/opinions dropped). Uses `litellm.completion()` directly rather than a CrewAI `Agent`/`Task`/`Crew` — this is a plain utility call with no role-play or tool use, and calling litellm directly means `agents.llm.run_with_rate_limit_backoff` applies unchanged without a Crew wrapper. Plain-text `CLAIM: ...`-per-line output, parsed with a regex — same reasoning as Phase 4's Judge parser: avoids `output_pydantic`'s extra, unretried LLM call.
  - `evaluation/entailment_checker.py`: local NLI classification via `sentence-transformers`' `CrossEncoder`, same Chroma-candidates -> cross-encoder pattern `rag/reranker.py` uses for relevance, just a different pretrained head (entailment/contradiction/neutral). Reads the model's own `id2label` config to map to our three labels rather than assuming a fixed order.
  - `evaluation/pipeline.py`: `check_statement(agent_name, company, statement)` ties extraction + retrieval + entailment together — the one function applied uniformly to Bull, Bear, the Judge, and (later) deep-dive sub-agents, per spec.md's "no agent is exempt or self-certifying" constraint.
  - `evaluation/metrics.py`: macro precision/recall/F1 (`score_predictions`) for pipeline acceptance testing, plus `groundedness_pct` (fraction of checked claims labeled "supported") — a natural byproduct of this pipeline's output that Phase 8's dashboard will reuse rather than recompute.
  - `evaluation/test_set.json`: 30 hand-labeled claims (16 AAPL, 14 MSFT — 15 supported, 9 contradicted, 6 not_enough_evidence), built from real chunk text pulled directly out of each company's already-indexed Chroma collection (not invented from general knowledge) — e.g. Apple's FY2025 segment/product net-sales table, the EU's €500M DMA fine, Microsoft's FY2026 segment revenue growth. Contradicted claims are real facts with the direction or a specific number flipped; not_enough_evidence claims are plausible-sounding but fabricated specifics (subscriber counts, JVs, pay raises) that don't appear in either filing.
  - **Real bug: naive indexing crashed entailment classification.** First implementation did `scores.argmax()` on a `(1, n_labels)` `CrossEncoder.predict()` result and indexed `scores[best_idx]` directly — for a single pair this flattens to shape `(1,)`, and any `best_idx >= 1` (i.e. contradiction/neutral winning) raised `IndexError: index out of bounds for axis 0 with size 1`. Fixed by taking `predict(...)[0]` (the row for that one pair) before `argmax`.
  - **Real bug: the entailment model can't resolve "the Company."** SEC 10-Ks name the company once on the cover page, then refer to it as "the Company" for the rest of the document — so a mid-filing chunk retrieved for a claim phrased with the real name ("Apple's total net sales were...") has no antecedent for the model to match against, and `cross-encoder/nli-deberta-v3-base` (and the smaller xsmall variant) scored these "neutral" (not_enough_evidence) at 85%+ confidence even when the chunk stated the fact verbatim. Verified directly: prefixing the evidence with one sentence naming the company (`evaluation/entailment_checker.py`'s `_CONTEXT_PREFIX`) flipped these to ~98%+ confidence entailment with no effect on genuinely unrelated pairs. Also switched the model from `nli-deberta-v3-xsmall` to `nli-deberta-v3-base` — xsmall stayed weak on filing-table content even after the prefix fix.
  - **Real bug: flattened HTML tables read as unnatural text to the NLI model.** `mcp_server/tools/get_filing.py`'s `_strip_html` turns filing tables into prose with stray internal spaces around currency/percent tokens — "$ 416,161" and "6 %" instead of "$416,161"/"6%". Verified directly: swapping that exact spaced fragment for a naturally-formatted restatement of the identical fact flipped the model from ~92%-confidence neutral to ~99.8%-confidence entailment with nothing else changed. Fixed with a narrow, targeted regex (`_normalize_spacing`) that only touches whitespace directly adjacent to `$`/`%`, never reflows or rewords anything else.
  - **Real bug: trusting only the single top-reranked chunk per claim is fragile.** `rag/chunker.py`'s fixed-window chunking (1000 chars/200 overlap) sometimes lands a table's "Total" row in the same window as unrelated per-segment narrative — e.g. one chunk paired "Total net sales $416,161, up 6%" with "Greater China net sales decreased..." two sentences later, and that conflicting signal flipped the model from entailment to confident (93%) contradiction, even though the overlapping neighbor chunk (200 chars later) stated the same total cleanly and scored 99%+ entailment. Fixed in `evaluation/pipeline.py`'s `check_claim()`: retrieve a small pool of top-ranked candidates (not just rank 0) and keep whichever produced the highest-confidence decisive verdict, with a small tie-break bonus favoring the best-reranked candidate so a same-confidence lower-ranked false positive (see next bug) doesn't override it.
  - **Real bug: feeding the whole ~1000-char chunk as the NLI premise buries the one sentence that matters.** Confirmed via a sweep across `evaluation/test_set.json`: narrowing the premise to just the claim-relevant span (using the project's own `rag.embedder` bi-encoder — the same model chunks are indexed with — to pick the single sentence-like span most similar to the claim, plus one neighbor each side for continuity) measurably raised recall on "supported" claims that were previously scored not_enough_evidence purely from surrounding-chunk noise. Widening the retrieved-candidate pool too far (tried top_k up to 5) backfired — pulling in enough candidates increases the odds of a spurious high-confidence false match on an unrelated chunk (observed: a fabricated Apple claim scored 99% "contradicted" against an unrelated signature block that happened to share a name). Settled on `DEFAULT_TOP_K = 2` after sweeping top_k x confidence-threshold x tie-break-bonus combinations against the test set — see `evaluation/pipeline.py`'s module docstring for the full before/after numbers.
  - **Acceptance bar adjusted from spec.md's original 0.75, per spec.md's own allowance ("adjust threshold once real numbers come in").** Tuning history on `evaluation/test_set.json`: naive top-1-chunk/raw-premise pipeline scored F1 = 0.43 (supported-claim recall as low as 0.07 — most true facts scored not_enough_evidence). After the context-prefix and spacing-normalization fixes: F1 = 0.55. After adding multi-candidate retrieval: peaked around F1 = 0.58 before span-narrowing (widening candidates alone helped precision-limited cases but also introduced false positives). After adding embedding-based span narrowing: F1 = 0.64-0.68 depending on top_k/threshold/tie-break settings. Final settings (`DEFAULT_TOP_K = 2`, `MIN_DECISIVE_CONFIDENCE = 0.6`, `TOP_RANK_BONUS = 0.1`) measure **F1 = 0.68** (precision 0.76, recall 0.75) — `scripts/test_phase6_evaluation.py`'s `MIN_F1` is set to 0.65, a real, measured bar with a small margin, not the original aspirational 0.75. This is the honest ceiling of a fully local, un-fine-tuned NLI model checking free-form claims against noisy, table-heavy 10-K prose — a fine-tuned or larger (e.g. FEVER-style fact-verification) model would likely do better but wasn't in scope for this pass. Remaining failure mode is systematically safe, not reckless: "supported" claims have 100% precision but only 47% recall (real facts sometimes conservatively scored not_enough_evidence), and not_enough_evidence has 100% recall (nothing fabricated ever slipped through as supported in this test set) at 40% precision — i.e. the pipeline under-confirms rather than hallucinating confirmations.
  - `orchestration/debate_loop.py` now calls `index_company_filing(company)` once at the start of `run_debate()` (claim-checking needs something to retrieve against) and `evaluation.pipeline.check_statement` on every Bull/Bear statement and the Judge's own memo — the returned `DebateResult` dict gained a `claim_details` field (`{claim_id: Claim}`, not in spec.md's original schema but a natural extension so transcript entries' `claims: [claim_id, ...]` lists are dereferenceable). The Judge's `claims_checked`/`claims_unsupported` (Phase 4: self-reported from its own reading of the transcript) are now replaced with real counts measured by the shared pipeline over every Bull/Bear claim in the debate, rather than the Judge grading its own homework.
  - **Not yet re-verified end-to-end with real Groq calls**: `run_debate()`'s wiring was verified by code review and by running the claim-checking pipeline standalone against real indexed filings (the acceptance script), but a full live debate now issues one extra Groq call per statement (claim extraction) on top of Phase 4's existing opening/rebuttal/verdict calls — worth re-running `python -m scripts.test_phase4_debate AAPL` (or an equivalent full-debate script) to confirm this doesn't blow the free-tier per-minute/per-day token budget documented in Phase 4's section above.

- **Phase 7 — deep-dive spawner:**
  - `deep_dive/section_splitter.py`: `split_sections(raw_text)` splits a 10-K into per-Item records using SEC's standard `Item N.` header structure (spec.md section 7), falling back to `_fallback_split` (4 roughly-equal character-length parts, snapped to sentence boundaries) when fewer than `MIN_STANDARD_HEADERS` real headers are found. `index_filing_sections(company, filing_type)` fetches the filing, splits it, and chunks + indexes each section separately in Chroma with `metadata["section"]` set to that section's label — reusing `rag.chunker.chunk_filing`'s existing (until now unused) `section` parameter, coexisting with Phase 5's whole-filing index (`section=""`) since `chroma_store.delete_filing_chunks` scopes deletes to `(filing_type, section)`.
  - `deep_dive/spawner.py`: `get_available_sections(company)` indexes and returns section labels (what Phase 9's UI generates one button per). `spawn_deep_dive(company, section, debate_result=None)` retrieves section-filtered excerpts (`rag.retriever.retrieve(..., where={"section": section})` — added a `where` passthrough param to `retrieve()` for this), runs a scoped CrewAI sub-agent to summarize them (never calls `get_filing` directly — fed only the retrieved excerpts, same rate-limit-conscious pattern as Bull/Bear's rebuttal turns and the Judge), claim-checks the summary via `evaluation.pipeline.check_statement("deep_dive_sub_agent", ...)`, and caches the full result per `(company, filing_type, section)` in an in-memory dict. If given a `debate_result` dict, merges the section into `deep_dive_available_sections`/`deep_dive_results` and folds claims into `claim_details` — "merge sub-agent findings back into the main debate context" per the roadmap.
  - **Real bug: naive TOC-vs-real-header disambiguation picked the wrong occurrence for two items.** First attempt kept whichever occurrence of each item number had the largest gap to the next distinct header (reasoning: TOC rows are packed tightly, real sections are followed by lots of prose). This broke for items whose real body content is trivially short — "Item 16. Form 10-K Summary None." right before the signature block, and "Item 11. Executive Compensation" (deferred entirely to the proxy statement) — because the TOC's *own* trailing gap (a page or two of legal preamble before Item 1 starts) can be larger than a two-sentence real section's trailing gap. Verified on AAPL: TOC's "Item 16." had a 2052-char gap vs. the real section's 2025-char gap — close enough to flip the heuristic. Fixed by detecting the TOC as a structural feature instead — a dense *run* of matches starting at the very first match, each within `TOC_GAP_THRESHOLD` (300 chars, calibrated off AAPL's real TOC-internal gaps, all under 230 chars) of the previous one — and excluding the whole run outright, then keeping the first occurrence of each item number among what's left (real section always precedes any later cross-reference).
  - **Real bug: MSFT's real section headers use ALL CAPS ("ITEM 1A. RIS K FACTORS") while its TOC uses Title Case ("Item 1A. Risk Factors") — the original regex only matched literal "Item".** Every one of MSFT's real body headers was silently invisible to it; the only matches found were TOC rows plus harmless "PART I Item 1"-style running page headers (no trailing period, already excluded), leaving nothing but TOC rows to split on — one entire section ("Item 16") swallowed 317,984 of the document's 323,531 chars. Fixed with an explicit `(?:Item|ITEM)` alternation rather than a blanket `re.IGNORECASE`, which would also have weakened the "next char is uppercase" check used to confirm a real heading rather than an in-sentence lowercase mention. Verified against both AAPL (22 sections, correctly ordered, no section over ~68k chars) and MSFT (22 sections, all pulling from real body content, largest section a legitimate 103k-char Item 8) after the fix.
  - **Real bug, found via "Claims checked: 0" on an obviously multi-claim deep-dive summary: `openai/gpt-oss-20b` (`agents/llm.py`'s `GROQ_MODEL`) is a reasoning model that can spend its entire completion-token budget on hidden `reasoning_tokens` before ever emitting a visible answer.** Root-caused directly: extracting claims from a real deep-dive summary returned `content=""` with `finish_reason="length"` and `reasoning_tokens=798` out of an 800-token budget; raising the budget to 1500 didn't help (1498 reasoning tokens, still empty) — this wasn't a rate-limit or malformed-request failure, so nothing in `run_with_rate_limit_backoff` had a reason to retry it, and it produced zero claims silently rather than erroring. Fixed by passing `reasoning_effort="low"` to the Groq call — confirmed this drops reasoning-token usage to ~200 on the same input and reliably leaves the requested output in the visible answer, without needing a much larger (and more rate-limit-expensive) token budget. Fixed in two places: `evaluation/claim_extractor.py`'s direct `litellm.completion()` call, and — since every other agent (Bull, Bear, Judge, and now the deep-dive sub-agent) shares `agents/llm.py`'s `build_groq_llm`, which goes through the identical Groq model — added `reasoning_effort="low"` there too. This is very likely the real root cause of Phase 4's previously-unresolved "Judge memo truncation" issue (same model, same failure shape), though a fresh full-debate run hasn't been done yet to confirm that specifically — see the Known issues entry below.
  - **False lead worth recording so it isn't re-chased**: while investigating the above, a debug script run from outside the project directory (a temp scratchpad path) hit a spurious Groq 401 "Invalid API Key" and briefly looked like a `litellm.completion(api_key=...)` bug. It wasn't — `python-dotenv`'s `find_dotenv()` searches upward from the *calling script's own file location*, not the process's working directory, so a script living outside the repo never finds `.env` and silently passes `api_key=None`. Confirmed by re-running the identical call from inside the project directory, where it succeeds normally. No code changed for this one; just don't debug this project's env-dependent code from scripts placed outside the repo root.
  - **Acceptance methodology**: `scripts/test_phase7_deepdive.py` mirrors spec.md's Phase 7 criterion directly rather than building a separate harness — index a real filing, "click" a (real, content-rich) section once and time it, "click" the same section again and assert both a cache hit (`cached=True`) and near-instant response (< 2s, well under the ~15s a real reprocess takes). Verified on AAPL: 22 sections found (need >= 3), first call 14.6s producing a 4-sentence summary citing 4 specific retrieved facts with all 4 claims making it through the entailment pipeline, second call 0.00s returning an identical result.

- **Phase 8 — Evaluation metrics dashboard:**
  - Deliberately thin: five of the six metrics reuse a module already built for an earlier phase rather than reimplementing measurement logic — `evaluation/dashboard.py` is glue, not new science. Only debate engagement scoring is genuinely new (`evaluation/engagement_scorer.py`), since nothing scored that before.
    1. **Groundedness % per agent** — `compute_groundedness_by_agent` groups a `DebateResult`'s `claim_details` by `claim["agent"]` and runs Phase 6's existing `evaluation.metrics.groundedness_pct` per group.
    2. **Judge accuracy (F1)** — reuses `evaluation.pipeline.check_claim` + `evaluation.metrics.score_predictions` against `evaluation/test_set.json`, the exact measurement `scripts/test_phase6_evaluation.py` already validates — spec.md's project-structure comment already scoped that test set as "for Judge F1 validation," since Phase 6's entailment pipeline is what backs the Judge's `claims_checked`/`claims_unsupported` counts (via `debate_loop.py`'s Phase 6 wiring), not a new hand-labeled Judge-verdict set.
    3. **Debate engagement score** — new `evaluation/engagement_scorer.py`: one Groq call (not one per statement, to stay within the free-tier budget) scores every round >= 2 transcript entry 1-5 against the statement immediately before it in speaking order, using the same plain-text-line-parsed-by-regex pattern as `agents/judge_agent.py` and `evaluation/claim_extractor.py` (avoids `output_pydantic`'s extra, unretried call). Round 1 entries are skipped — independent openings have nothing to engage with yet, by design.
    4. **Retrieval recall@k, pre vs post rerank** — `evaluation/retrieval_metrics.py`. The sampling method (sample chunks spread across an indexed filing, treat a distinctive mid-chunk sentence as a "question") already existed in `scripts/test_phase5_retrieval.py`'s `_sample_test_questions`; moved (not duplicated) into this new module as `sample_test_questions`, and that script now imports it. Added `recall_at_k(company, k, n_questions)`, which reuses the same sampled questions for both measurements (raw Chroma vector order vs `rag/reranker.py`'s cross-encoder order) so the pre/post comparison isolates reranking's effect rather than sampling noise between two separate runs.
    5. **Latency / LLM calls per debate** — needed new instrumentation, since nothing tracked this before. `agents/llm.py`'s `run_with_rate_limit_backoff` gained an optional `label` param and a process-global `_CALL_LOG` list (`reset_call_log()`/`get_call_log()`) that records one `{label, duration_seconds, timestamp}` entry per successful logical call (not per retry attempt). Every call site was given a label: `bull_opening`/`bull_rebuttal`, `bear_opening`/`bear_rebuttal`, `judge_verdict`, `claim_extraction`, `deep_dive_summary`. `evaluation/dashboard.py`'s `summarize_latency` takes a wall-clock duration plus a call log (both captured by the caller bracketing a debate run — the dashboard module has no hook into when a debate starts/ends) and breaks the call count down by label, so a deep dive's added calls are visible separately from the base debate's.
    6. **Deep-dive lift** — `compute_deep_dive_lift` measures `groundedness_pct` over all of `claim_details` before vs after merging one `deep_dive.spawner.spawn_deep_dive` result in (on a copy of the debate result, not mutating the caller's).
  - **Acceptance run** (`python -m scripts.test_phase8_dashboard AAPL`, real Groq calls): a full 3-round debate + one deep-dive spawn finished in 206.8s over 14 LLM calls (1 bull opening, 1 bear opening, 2 bull rebuttals, 2 bear rebuttals, 1 judge verdict, 7 claim-extraction calls — one per transcript statement including the Judge's memo). All six metrics computed without error:
    - Groundedness: bull 35%, bear 36%, judge 0% (small sample — the Judge's memo produced few extractable claims in this run, not necessarily a bug; worth re-checking across more runs before treating 0% as meaningful).
    - Judge accuracy: accuracy=0.60, precision=0.74, recall=0.70, **f1=0.62**.
    - Engagement: 5.00/5 average across all 4 round-2+ statements — every rebuttal named a specific opponent claim before responding, consistent with Phase 4's original manual spot-check.
    - Retrieval recall@3: 80% pre-rerank and 80% post-rerank (n=10) — reranking made no difference on this particular sample; not the same as Phase 5's 10/10 result since the sampling here draws a fresh set of 10 questions each run rather than reusing Phase 5's fixed sample.
    - Latency: 206.8s wall clock, 14 calls, 1.3s average call duration — the gap between wall-clock and summed call time is mostly `INTER_TURN_PAUSE_SECONDS` (15s x 6 inter-turn pauses = 90s) plus local model loading (embedder + cross-encoder + NLI model) at the start, none of which run through `run_with_rate_limit_backoff`.
    - Deep-dive lift: groundedness 31% -> 36% (+4%) after merging one "Item 1A — Risk Factors" deep dive in.
  - **Open discrepancy, not yet chased down**: this run's Judge-accuracy F1 (0.62) is measurably below the 0.68 Phase 6 originally recorded on the identical test set and pipeline (`evaluation/test_set.json`, `evaluation.pipeline.check_claim`) — see Phase 6's write-up above. Both `check_claim` and the underlying NLI/reranker models are deterministic (no sampling), so this shouldn't drift between identical runs; worth a direct re-run of `scripts/test_phase6_evaluation.py` to confirm whether this is a real regression (e.g. a dependency version change since Phase 6) or this particular run hit a different code path (e.g. Chroma collection state left over from something else touching AAPL's/MSFT's index earlier in the same script run). Not chased further here since the immediate criterion (all six metrics computed) is met regardless of the exact F1 value.

- **Phase 9 — Frontend (Streamlit):**
  - `frontend/app.py` talks directly to `orchestration.debate_loop` and `deep_dive.spawner` in-process — no FastAPI/Redis/worker layer, since progress.md's architecture diagram already scopes that queue/worker pattern as "added later" for Phase 10's deployment, not required for a working single-user UI.
  - **"Updates live as a debate runs" needed `orchestration/debate_loop.py` refactored, not just a new frontend file.** `run_debate()` previously blocked until the entire debate finished before returning anything, so there was nothing to render incrementally. Split it: `run_debate_stream(company, rounds)` is now the actual generator implementation, yielding one event per step (`indexed` -> `statement` x N -> `verdict` -> `done`); `run_debate()` is a thin wrapper that drains the generator and returns the same `DebateResult` dict as before. Every existing caller (`scripts/test_phase4/6/7/8_*.py`) needed no changes — confirmed by re-importing the module and checking both functions are still callable.
  - `frontend/app.py` consumes `run_debate_stream` directly and renders each `st.chat_message` bubble (🐂 bull, 🐻 bear, ⚖️ judge) as its event arrives, rather than calling `run_debate()` and waiting — Streamlit flushes each `st.*` call to the browser as the script executes, so this alone is enough for live updates without threads/async/websockets of our own.
  - Deep-dive buttons: one per section from `deep_dive.spawner.get_available_sections(company)`, cached in `st.session_state` per debate so it isn't recomputed on every Streamlit rerun (every widget interaction reruns the whole script top to bottom). Clicking a button calls `spawn_deep_dive(company, section, debate_result=result)`, which mutates the same dict held in `st.session_state.debate_result` in place (per `deep_dive/spawner.py`'s existing merge behavior) — no extra plumbing needed to persist the result across the rerun a button click triggers.
  - Memo card shows `stronger_side`, the memo text, and a fact-check line (`supported/checked` claims, unsupported count) computed directly from `judge_verdict`'s existing fields — no new computation, matches spec.md's "final memo card with verdict and fact-check count" acceptance wording directly.
  - **`streamlit run` needed an explicit sys.path fix.** Unlike `python -m scripts.foo` (which puts the project root on `sys.path` via the CWD), `streamlit run frontend/app.py` only adds `frontend/`'s own directory, so `import orchestration.debate_loop` and friends would otherwise fail. Fixed with a `sys.path.insert(0, ...)` at the top of `frontend/app.py` inserting the project root (the file's grandparent directory) before any project-internal imports.
  - **Verified: the app boots.** `streamlit run frontend/app.py --server.headless true` on a test port returned HTTP 200 with a clean startup log (no import errors, no path errors, no Streamlit warnings). This confirms the module imports correctly and the initial page renders.
  - **Not yet verified: clicking "Run debate" and a deep-dive button end to end in a real browser.** That would need actually driving the UI (typing a ticker, clicking through a live multi-minute debate, clicking a deep-dive button) — no browser-automation tool was available in this session to do that interactively. The debate/deep-dive logic itself is already proven end-to-end via `scripts/test_phase4_debate.py`, `scripts/test_phase7_deepdive.py`, and Phase 8's dashboard script (all real Groq runs), and `run_debate_stream`'s only change from the previously-verified `run_debate` is restructuring *when* each already-tested piece of data is yielded, not what it computes — but the Streamlit-specific rendering path (chat bubbles appearing incrementally, session-state persistence across a deep-dive button's rerun) has not itself been click-tested. Do that before treating Phase 9 as fully done, not just "boots."

## Known issues / open bugs

Bugs and rough edges hit so far, not all fully resolved:

- **Confirmed live hallucination: an agent stated a claim with zero basis in the retrieved tool output — nothing catches this today.** Running `python -m scripts.test_phase3_agents MSFT` (2026-08-30), Bear's opening statement asserted *"a slowdown in cloud revenue growth and a modest decline in operating margin"* as if it were a retrieved fact from Microsoft's 10-K. Checked directly: `get_filing_tool`'s actual output for that run was only the filing's cover page (2500 chars — company info, ticker, notes due dates, filer-status checkboxes); it contains no mention of "cloud," "margin," or "slowdown" anywhere. The model filled the gap from its own pretraining knowledge of Microsoft, not from anything it retrieved, directly violating `BEAR_SYSTEM_PROMPT`'s "only make claims that are backed by retrieved evidence" instruction. That instruction is currently prompt-level only — nothing enforces or checks it. This is precisely the gap **Phase 6** (claim extraction -> retrieval -> entailment pipeline) is designed to close; until that's built, treat every Bull/Bear/Judge claim as unverified regardless of how confidently it's phrased or cited. Worth using this exact example (MSFT Bear, "cloud revenue growth"/"operating margin" claim) as a regression case once Phase 6's entailment checker exists — it should flag it as `not_enough_evidence` or `contradicted`.
- ~~Bull/Bear/Judge still call `get_filing_tool` and its 2500-char cover-page truncation directly — not yet wired to Phase 5's `rag.retriever.retrieve()`~~ **Fixed: opening statements and the verdict now run on pre-fetched RAG retrieval, not raw tool output.**
  - **Why this couldn't be "just make the agent call `retrieve()` as a tool" instead:** `max_iter=2` (one tool-call turn, one final-answer turn — see Phase 3's design decisions log) is a hard budget from Groq's free-tier token cap. A good opening statement needs evidence from more than one angle (growth signal for Bull, risk signal for Bear), which is naturally 2+ retrieval queries — more than the iteration budget affords if the *agent* is the one issuing the calls.
  - **Fix:** pre-fetch retrieval context before invoking the agent and inject it into the task description as text — the same pattern `deep_dive/spawner.py` already used for its section sub-agents (retrieve first, hand the agent only the excerpts, tell it not to call tools). No new tool call, no extra iteration budget spent.
  - `agents/bull_agent.py` / `agents/bear_agent.py`: added `_build_retrieved_context(company, queries)`, called with role-specific queries (`BULL_OPENING_QUERIES` = growth/competitive-strength angles, `BEAR_OPENING_QUERIES` = risk/decline angles) before `run_opening_statement`'s task is built. Returns `""` (not a placeholder sentence) if every query comes back empty, so the caller can branch cleanly.
  - **Empty-retrieval edge case, caught in review before landing:** the original draft always instructed "cite at least 2 facts from the excerpts," which collides with an empty index (nothing indexed yet) or a query that just doesn't match anything for a given filer. Fixed by branching the task instruction itself — when `_build_retrieved_context` returns `""`, the agent is instead told to state plainly that no evidence was retrievable and assert no specific facts, rather than being pushed toward inventing some to satisfy the citation requirement.
  - **The Judge had a related but distinct gap, caught in a follow-up review**: it never called `get_filing_tool` either, but its problem was "no source at all" (judges from transcript prose alone) rather than Bull/Bear's "wrong source" (cover page only). `debate_loop.py`'s `check_statement` pass over the Judge's own memo (Phase 6) measures groundedness of the memo's claims after the fact, but doesn't give the Judge anything to check *against* while forming the verdict in the first place. Fixed the same way: `agents/judge_agent.py` now pre-fetches `JUDGE_EVIDENCE_QUERIES = BULL_OPENING_QUERIES + BEAR_OPENING_QUERIES` (both angles, since the Judge needs to check both sides) and injects it into the verdict task, falling back to transcript-only judging (its original Phase 4 behavior) when retrieval is empty.
  - `agents/prompts.py`'s `BULL_SYSTEM_PROMPT`/`BEAR_SYSTEM_PROMPT` updated from "you have access to tools that retrieve..." to "you will be given retrieved excerpts..." to match — was stale flavor text after this change (harmless since the task description already overrode it at runtime, but inaccurate as backstory).
  - `agents/tools.py`'s `get_filing_tool` and `ALL_TOOLS` are unchanged and still attached to every agent — still needed to dodge the Groq zero-tools `tool_choice: "none"` 400 bug (Phase 4 design decisions log); `get_filing_tool` itself is just no longer called by these tasks.
  - **Not yet re-verified end-to-end with a live Groq run** — confirmed only via import smoke tests so far. Run a real debate (e.g. `python -m scripts.test_phase4_debate <TICKER>`) to confirm the empty-context fallback path and the Judge's new evidence block don't break the existing verdict parsing or blow the token budget.
- ~~`get_filing`'s underlying raw text starts with tens of thousands of characters of inline-XBRL tag noise before real prose begins~~ **Fixed at the source, verified against smaller/foreign filers plus a content diff.** `mcp_server/tools/get_filing.py`'s `_strip_html` now decomposes any element with inline `display:none` styling (verified against real EDGAR HTML: the hidden `<ix:header>` block — fund identifiers, GAAP taxonomy refs, duplicate/hidden facts — is wrapped in `style="display:none"`, while visible inline-tagged values like `ix:nonfraction` sit in normally-styled elements and must not be touched) plus an explicit `ix:header` decompose as a belt-and-suspenders fallback.
  - Verified the fix against 7 filings, not just the 3 large standard filers: AAPL/MSFT/TSLA (large accelerated filers), **RIOT and NVAX (smaller-cap 10-K filers)**, and **BABA and TM (foreign private issuers, 20-F)**. Cover-page text now starts within ~30-80 characters of `raw_text` for all 7 (was 14k-90k chars in before, worse for the bigger foreign filings).
  - **This surfaced two real bugs in the first pass of the fix, caught specifically by testing RIOT's messier HTML** (exactly the kind of filer the smaller/less-standardized hypothesis predicted would break it): (1) `tag["style"]` crashed with `TypeError: 'NoneType' object is not subscriptable` on a bare `style` attribute with no value; (2) decomposing a `display:none` parent sets bs4's `.attrs = None` on the whole subtree, but `soup.find_all(style=True)` is computed once up front, so a nested `display:none` child (RIOT's HTML nests them) still showed up in the loop after its parent was already gone, crashing the same way. Both fixed with a `tag.attrs is None: continue` guard before touching `tag.attrs.get("style")`.
  - **Ran a content diff, not just a "starts near character 0" check**, to make sure the `display:none` sweep isn't eating real hidden-but-legitimate disclosure text (some filers do hide real boilerplate via CSS, not just XBRL tags). For each filing: compared old-strip vs new-strip word sets, captured the actual text of every decomposed element, and ran a prose-heuristic (common-word ratio) over each removed block. Result across all 7 filings: **zero removed blocks flagged as prose-like**; every "lost" word is an unspaced XBRL/GAAP taxonomy element name (e.g. `accumulatedothercomprehensiveincomemember`, `commonstockmember`), never real vocabulary. The heuristic itself was sanity-checked against a real legal-boilerplate sentence (correctly flagged prose-like) and a taxonomy-name string (correctly flagged not).
  - `agents/tools.py`'s `get_filing_tool` no longer needs its marker-search workaround — simplified back to a plain prefix truncation. Phase 5's chunker inherits clean text for free.
- **`crewai` 1.15.18's `cache_breakpoint` bug is a monkeypatch, not a real fix** (`agents/llm.py`, patches `crewai.llms.cache.mark_cache_breakpoint`). It works because both crewai executors re-import that name locally per call, but it's brittle to a crewai internals change — pin crewai's version or re-verify this patch still applies before upgrading crewai later.
- **Windows console can't print LLM output containing smart quotes/en-dashes by default.** `python scripts/test_phase3_agents.py` crashed with `UnicodeEncodeError` on cp1252 when printing a Bull statement containing `’`/`‑` characters. Fixed locally in that script with `sys.stdout.reconfigure(encoding="utf-8")`, but any other CLI script (or the Phase 10 worker's logging) that prints raw LLM output on Windows will need the same fix — not yet applied project-wide.
- **The `mcp` package version is fragile to reinstall order.** Installing `crewai` silently downgraded `mcp` from v2.1.1 to v1.28.1 as a transitive dependency, which broke `mcp_server/server.py` (`MCPServer` renamed/moved to `FastMCP`) until caught and fixed. If `requirements.txt` is ever reinstalled fresh in a different order, or `crewai` is upgraded, re-run `scripts/test_mcp_tools.py` to confirm the MCP server still boots.
- **Groq's free-tier rate limit is a standing constraint, not a one-off bug — confirmed worse in Phase 4.** A full 3-round debate (7 LLM calls: 2 openings + 4 rebuttals + 1 verdict) reliably brushes up against both the 8000 tokens/minute cap and, after enough same-day testing, the 200,000 tokens/day cap. Current mitigations (trimmed tool output, `max_iter=2`, single-tool-call tasks, 15s inter-turn pauses, retry-with-backoff covering `RateLimitError`/`ValueError`/`BadRequestError`) get one full debate through most of the time, but this account's daily quota was fully exhausted by this session's testing — see the Phase 4 design decisions log above. A real production strategy (paid tier, spreading debates across time, or a much cheaper model for rebuttal/judge turns) is needed before this scales past manual testing.
- **Bull/Bear rebuttal and Judge turns still have real tools attached (`tools=ALL_TOOLS`) even though they're instructed not to call them**, as a workaround for the zero-tools `tool_choice: "none"` 400 bug (Phase 4 design decisions log). This is a prompt-level instruction, not an enforced constraint — nothing stops the model from calling a tool anyway on a given turn, which would both cost extra tokens and go against the "read the transcript, don't fetch new evidence" design intent. Watch for this in transcripts (a rebuttal statement introducing a fact that wasn't in any prior round is the tell).
- **Phase 4's Judge memo-truncation fix and the daily-cap fail-fast haven't been re-verified together end-to-end** — the daily quota ran out before a fresh run could confirm them. Re-run `python -m scripts.test_phase4_debate AAPL` once the quota resets (see Phase 4 design decisions log for details). **Likely already fixed as a side effect of Phase 7's `reasoning_effort="low"` fix to `agents/llm.py`'s `build_groq_llm`** — the root cause found in Phase 7 (GROQ_MODEL silently burning its whole completion-token budget on hidden reasoning tokens, leaving the visible answer empty/truncated) applies to every agent built via `build_groq_llm`, including the Judge, not just the Phase 7 claim extractor it was diagnosed in. Still needs a fresh `test_phase4_debate` run to confirm the memo comes through complete now.

- **Judge-accuracy F1 (Phase 8's dashboard metric) measured 0.62 in one real run, vs Phase 6's originally-recorded 0.68 on the identical hand-labeled test set and deterministic pipeline.** See Phase 8's design decisions log entry above for the full context. Not yet root-caused — re-run `scripts/test_phase6_evaluation.py` directly to confirm whether this is a real regression or an artifact of this particular run's Chroma/model state.
- **Phase 9's Streamlit app has only been boot-tested (imports cleanly, serves HTTP 200), not click-tested in a real browser.** "Run debate" (a multi-minute live Groq run rendered incrementally) and the deep-dive buttons (session-state persistence across a Streamlit rerun) haven't been interactively verified. See Phase 9's design decisions log entry for what's already proven vs what specifically still needs a manual click-through.

## Not yet decided / deferred

- The second (general-domain) project — intentionally set aside for a future session
- Whether to eventually randomize debate turn order
- Whether to add an automatic deep-dive trigger (e.g. if too many unsupported claims are flagged) — deferred as unnecessary complexity for v1
